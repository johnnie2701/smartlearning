<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme, forced to Light with no default Action Bar -->
    <style name="Theme.SmartLearning" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Theme Overlay for a Toolbar with white text/icons -->
    <style name="ToolbarTheme.WhiteText" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <!-- This theme forces dark action bar components (like white text and icons) -->
        <!-- You can also override specific attributes here -->
        <item name="android:textColorPrimary">@android:color/white</item> <!-- For title text -->
        <item name="android:textColorSecondary">@color/white_secondary</item> <!-- For subtitle, overflow menu text, and often influences SearchView hint -->
        <item name="colorControlNormal">@android:color/white</item> <!-- For icons like overflow, navigation, and SearchView icons -->
        <item name="android:editTextColor">@android:color/white</item> <!-- Should influence SearchView input text -->
        <item name="android:textColorHint">@color/white_hint</item> <!-- Should influence SearchView hint text -->
    </style>

    <!-- Define custom colors if needed -->
    <color name="white_secondary">#E0E0E0</color> <!-- A slightly off-white for secondary text -->
    <color name="white_hint">#B0B0B0</color>    <!-- A lighter gray/white for hints -->
    <color name="your_primary_color">#FF6200EE</color> <!-- Example primary color -->
</resources>